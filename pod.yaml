apiVersion: apps/v1
kind: Deployment
metadata:
  name: 21-07-api
spec:
  replicas: 2
  selector:
    matchLabels:
      deployment-name: 21-07-api
  template:
    metadata:
      labels:
        deployment-name: 21-07-api
    spec:
      containers:
        - name: 21-07-api-container
          image: iurib8it148.azurecr.io/buildingtesting:945d09a9bcf61f3b487ad91daf40e8cfea85a6fd
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  labels:
    service-label: ionic-cluster-service
  name: ionic-cluster-service
spec:
  type: ClusterIP
  ports:
  - name: ionic-cluster-service-port
    port: 3000
    protocol: TCP
  selector:
    deployment-name: api-clusterIP
---
apiVersion: v1
kind: Service
metadata:
  labels:
    service-label: ionic-cluster-service-loadbalancer 
  name: ionic-cluster-service-loadbalancer
spec:
  type: LoadBalancer
  ports:
  - name: load-balancer-service-port
    port: 3000
    protocol: TCP
  selector:
    deployment-name: api-loadBalancer